//------------------------------------------------------------------------------------------------------
//------------------------------------------------------------------------------------------------------

#include "Gfx.h"
#include "Core/App.h"

//------------------------------------------------------------------------------------------------------
//------------------------------------------------------------------------------------------------------

namespace Game
{
    //------------------------------------------------------------------------------------------------------
    //------------------------------------------------------------------------------------------------------

    Gfx::Gfx()
    {
        m_timePerFrame      = sf::seconds(1.0f / DESIRED_FPS);
        m_pixelsPerMetre    = 64;
        m_worldGravity      = GDX::Vec2F(0, 9.8f);
        m_lerpSpeed         = 0.0175f;
        m_DesktopWidth      = 1120;         // Dimensions of the game window
        m_desktopHeight     = 640;
        m_viewWidth         = 2560;         // Dimensions of the game camera(s) viewport
        m_viewHeight        = 1440;
        m_hudWidth          = 1280;         // Dimensions of the HUD camera viewport
        m_hudHeight         = 720;
    }

    //------------------------------------------------------------------------------------------------------
    //------------------------------------------------------------------------------------------------------

    // TODO - unfinished
    int Gfx::VisibleMapWidth() const
    {
        return pApp->m_pMapData->m_mapWidth - m_viewWidth;
    }

    //------------------------------------------------------------------------------------------------------
    //------------------------------------------------------------------------------------------------------

    // TODO - unfinished
    int Gfx::VisibleMapHeight() const
    {
        return pApp->m_pMapData->m_mapHeight - m_viewHeight;
    }
}

